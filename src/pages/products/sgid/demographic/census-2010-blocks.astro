---
import Layout from '@layouts/DataPage.astro';

import type { IMetadata, IPageMetadata } from '@models/products/sgid/types';
import { ProductType, SgidCategory } from '@models/products/sgid/types';

import HubDownloads from '@components/data/HubDownloads.astro';
import Metadata from '@components/data/Metadata.astro';
import UpdateHistory from '@components/data/UpdateHistory.astro';

import CardWithSmallLink from '@components/page/CardWithSmallLink.astro';
import GridForYouMightLike from '@components/page/GridForYouMightLike.astro';
import Section from '@components/page/Section.astro';

import { dataPages } from '@data/downloadMetadata';

import Census2010Description from '@partials/Census2010Description.astro';
import Census2010Links from '@partials/Census2010Links.astro';

export const metadata: IMetadata = {
  title: 'Census 2010 Blocks',
  description: `The smallest geographic unit with very basic demographic data`,
  stewards: ['UGRC', 'US Census Bureau'],
  type: ProductType.POLYGON,
  category: SgidCategory.DEMOGRAPHIC,
};

const hubTitle = 'Utah Census Blocks 2010';

const page: IPageMetadata = {
  ...metadata, // this brings all the data from the metadata object above into the page object
  updateHistory: [`August 2021: Initial load`],

  hub: {
    ...dataPages[hubTitle],
  },
};
---

<Layout title={page.title} subTitle={page.updateHistory[0]} category={page.category} actionUrl={page.application}>
  <Metadata slot="metadata" {...page} />

  <Fragment slot="summary">
    <p>
      Census Blocks are the smallest geographic unit used by the United States Census Bureau for tabulating population
      information. To preserve privacy, only a very limited amount of data is available at the block level.
    </p>
  </Fragment>

  <Section title="Use the data" slot="downloads" titlePosition="center">
    <HubDownloads {...page.hub} />
  </Section>

  <Fragment slot="description">
    <Census2010Description />
  </Fragment>

  <UpdateHistory slot="update-history" updates={page.updateHistory} style="secondary" />

  <Section title="More resources" slot="more-resources" titlePosition="center">
    <Census2010Links />
  </Section>

  <Section title="You might also like" slot="you-might-also-like" stripe titlePosition="center">
    <GridForYouMightLike>
      <CardWithSmallLink
        title="2020 Census Blocks"
        href="/products/sgid/demographic/census-2020-blocks"
        style="secondary">Blocks from the 2020 Census</CardWithSmallLink
      >
      <CardWithSmallLink title="2010 Tracts" href="/products/sgid/demographic/census-2010-tracts" style="secondary"
        >A common Census aggregation level with widely available information</CardWithSmallLink
      >
    </GridForYouMightLike>
  </Section>
</Layout>
