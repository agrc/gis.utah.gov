---
import type { IStandardPageMetadata } from '@models/products/sgid/types';

import QuickLinks from '@components/page/QuickLinks.astro';
import Section from '@components/page/Section.astro';
import Layout from '@layouts/FullWidth.astro';

import { Image } from 'astro:assets';

import eventsPhoto from '@images/collaboration/events.svg';

import { stripeSections } from '@utils/page';

const page: IStandardPageMetadata = {
  title: 'GIS events',
  section: stripeSections([
    {},
    {
      title: 'Maps on the hill (MOTH)',
      actionUrl: '/collaboration/events/maps-on-the-hill',
      actionText: 'Link text... can be empty for default value',
    },
  ]),
};
---

<Layout title={page.title}>
  <QuickLinks links={page.section} slot="quick-links" />
  <Section {...page.section[0]}>
    <Image
      slot="image"
      src={eventsPhoto}
      loading="eager"
      alt="a person circling a date on a calendar"
      class="w-full max-w-[400px] rounded-2xl"
    />
    Utah is home to the Utah Geographic Information Council conference. Usually one is along the Wasatch Front and the other
    is in rural Utah on alternating years. There are also other opportunities to show off your work, such as Maps On The
    Hill (MOTH) during the legislative session each year.
  </Section>
  <Section {...page.section[1]}>
    <ul>
      <li>UGIC conference</li>
      <li>MOTH</li>
    </ul>
  </Section>
</Layout>
