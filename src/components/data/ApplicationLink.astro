---
import ButtonLink from '@components/page/ButtonLink.astro';

const { href } = Astro.props;
---

{
  href && (
    <div class="max-w-fit">
      <ButtonLink href={href} decorate="arrow">
        See the data in action
      </ButtonLink>
    </div>
  )
}
